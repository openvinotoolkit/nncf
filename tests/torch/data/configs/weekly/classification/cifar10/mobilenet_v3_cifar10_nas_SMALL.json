{
    "model": "mobilenet_v3_small",
    "num_classes": 10,
    "dataset": "CIFAR10",
    "input_info": {
        "sample_size": [1, 3, 32, 32]
    },
    "batch_size": 2,
    "batch_size_val": 2000,
    "multiprocessing_distributed": false,
    "bootstrapNAS": {
        "training": {
            "algorithm": "progressive_shrinking",
            "progressivity_of_elasticity": ["kernel", "depth", "width"],
            "batchnorm_adaptation": {
                "num_bn_adaptation_samples": 1
            },
            "schedule": {
                "list_stage_descriptions": [
                    {"train_dims": ["kernel"], "epochs": 1},
                    {"train_dims": ["kernel", "depth"], "epochs": 1, "depth_indicator": 1},
                    {"train_dims": ["kernel", "depth", "width"], "epochs": 1, "reorg_weights": true, "width_indicator": 2, "bn_adapt": true, "depth_indicator": 2}
                ],
            },
            "elasticity": {
                "enabled_elasticity_dims": ["width", "depth", "kernel"],
                "width": {
                    "max_num_widths": 2,
                      "min_out_channels": 32,
                      "width_step": 32,
                      "use_width_multiplier": true,
                      "width_multipliers": [1, 0.85]
                },
                "kernel": {
                    "max_num_kernels": "3"
                }
            }
        },
        "search": {
            "algorithm": "NSGA",
            "evaluations": 1000
        },
        "compression": [
            {
                "algorithm": "quantization",
                "params": {},
                "initializer": {}
            }
        ]
    }
}
